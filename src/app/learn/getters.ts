import { ILearnState } from './state';

export interface ILearnGetters {
  /**
   * put your getter names, parameters and return values here, for example:
   * myAction(context: ActionContext<ILearnState, IState>, param: any): Promise<any>;
   */
}

export const LearnGetters: ILearnGetters = {
  /**
   * here comes the implementation of your getters, for example:
   * myGetter(state: ILearnState) {
   *   return state.myAttribute;
   * }
   */
};

export interface ILearnState {
  /**
   * put your state attributes here, for example:
   * myAttribute: any;
   */
}

export const LearnDefaultState = (): ILearnState => {
  return {
    /**
     * put your default value here, for example:
     * myAttribute: null,
     */
  };
};

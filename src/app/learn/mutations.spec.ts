import { LearnMutations } from './mutations';
import { LearnDefaultState, ILearnState } from './state';

describe('LearnMutations', () => {
  let testState: ILearnState;

  beforeEach(() => {
    testState = LearnDefaultState();
  });

  test('Please write the tests for the mutations here', () => {
    // here is an example: https://github.com/vuesion/vuesion/blob/master/src/app/counter/mutations.spec.ts
    expect(false).toBeFalsy();
  });
});
